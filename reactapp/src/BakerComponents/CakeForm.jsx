import React,{useEffect,useState}from'react';import{useNavigate,useParams}from'react-router-dom';import api from'../apiConfig';export default function CakeForm(){const{id}=useParams(),nav=useNavigate(),[f,setF]=useState({name:'',category:'Birthday',price:'',quantity:'',cakeImage:''});useEffect(()=>{if(id)api.get('/api/cakes/'+id).then(r=>setF(r.data))},[id]);const change=e=>setF({...f,[e.target.name]:e.target.value});async function save(e){e.preventDefault();if(id)await api.put('/api/cakes/'+id,f);else await api.post('/api/cakes',f);alert(id?'Cake updated successfully':'Cake added successfully');nav('/baker/cakes')}return <main className="center"><form className="card" onSubmit={save}><h2>{id?'Edit':'Create New'} Cake</h2><input name="name" placeholder="Name *" required value={f.name} onChange={change}/><select name="category" value={f.category} onChange={change}><option>Birthday</option><option>Wedding</option><option>Anniversary</option><option>Chocolate</option></select><input name="price" type="number" min="0.01" step="0.01" placeholder="Price *" required value={f.price} onChange={change}/><input name="quantity" type="number" min="0.01" step="0.01" placeholder="Quantity *" required value={f.quantity} onChange={change}/><input name="cakeImage" type="url" placeholder="Cake image URL" value={f.cakeImage} onChange={change}/><button>{id?'Update Cake':'Add Cake'}</button><button type="button" onClick={()=>nav(-1)}>Back</button></form></main>}
