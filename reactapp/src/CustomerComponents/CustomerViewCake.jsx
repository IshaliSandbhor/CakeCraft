import React,{useEffect,useState}from'react';import api from'../apiConfig';export default function CustomerViewCake(){const[cakes,setCakes]=useState([]),[q,setQ]=useState('');useEffect(()=>{api.get('/api/cakes').then(r=>setCakes(r.data))},[]);const list=cakes.filter(c=>c.name.toLowerCase().includes(q.toLowerCase()));return <main><h2>Available Cakes</h2><input placeholder="Search by cake name" value={q} onChange={e=>setQ(e.target.value)}/><section className="grid">{list.length?list.map(c=><article className="cake" key={c.cakeId}><img src={c.cakeImage||'https://placehold.co/300x180?text=Cake'} alt={c.name}/><h3>{c.name}</h3><p>{c.category} | ₹{c.price}</p></article>):<p>Oops! No cakes found</p>}</section></main>}
